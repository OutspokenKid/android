<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >
    
    <!-- 타이틀바. -->
    
    <com.cmons.cph.views.TitleBar
	    android:id="@+id/wholesaleProfileImagePage_titleBar" 
	    android:layout_width="match_parent"
	    android:layout_height="0dp"
	    android:layout_alignParentLeft="true"
	    android:layout_alignParentTop="true" />

    <!-- 배경. -->
    
    <ImageView 
        android:id="@+id/wholesaleProfileImagePage_ivBg" 
	    android:layout_width="match_parent"
	    android:layout_height="match_parent"
	    android:layout_below="@id/wholesaleProfileImagePage_titleBar"
	    android:scaleType="centerCrop"
	    android:src="@drawable/shop_bg" />

    <!-- 매장 사진. -->
    
    <ImageView 
        android:id="@+id/wholesaleProfileImagePage_ivImage" 
	    android:layout_width="match_parent"
	    android:layout_height="0dp"
	    android:layout_below="@id/wholesaleProfileImagePage_titleBar"
	    android:scaleType="centerCrop"
	    android:src="@drawable/picture_default" />
    
    <!-- 사진 올리기. -->
    
    <Button
        android:id="@+id/wholesaleProfileImagePage_btnUpload"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/wholesaleProfileImagePage_ivImage"
        android:background="@drawable/picture_upload_btn" />
    
    <!-- 사진 설명. -->
    
    <TextView
        android:id="@+id/wholesaleProfileImagePage_tvImageText"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/wholesaleProfileImagePage_btnUpload"
        android:text="@string/shopProfileImageDescription"
        android:textColor="#fff"
        android:gravity="center_vertical" />
    
    
    
</RelativeLayout>